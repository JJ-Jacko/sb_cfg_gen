[project]
name = "sb_cfg_gen"
version = "1.0"
description = "The tools to generate sing-box config file"
authors = [
    { name = "Jacko", email = "l2982211930@163.com" }
]
dependencies = [
    "requests"
]

[project.scripts]
sb-gen-airport = "sb_cfg_gen.main:gen_airport"
sb-gen-diy = "sb_cfg_gen.main:gen_diy"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"